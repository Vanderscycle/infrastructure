apiVersion: apps/v1
kind: Deployment
metadata:
  name: server-factorio-server-charts
spec:
  template:
    spec:
      containers:
        - name: server-factorio-server-charts
          ports:
            - containerPort: 27015
              name: http
              protocol: TCP
