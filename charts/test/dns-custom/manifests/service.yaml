apiVersion: v1
kind: Service
metadata:
  name: dns-hello-world-service
  namespace: dns-hello-world
spec:
  selector:
    app: dns-hello-world
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
  type: LoadBalancer
